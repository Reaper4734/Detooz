# 🗄️ Detooz Archive

This directory contains deprecated, temporary, or one-off scripts and logs moved from the main `backend` directory during the cleanup phase (Jan 2026).

## Contents
*   **Logs**: `backend.log`, `error_log.txt`, `test_results.txt`.
*   **JSON Dumps**: API response snapshots.
*   **Scripts**:
    *   `test_*.py`: Unit tests for old bugs (Duplicate reg, OpenRouter, Qwen).
    *   `verify_*.py`: QA scripts for Scaling, OTP, Optimizations.
    *   `migrate_*.py`: Database schema migration scripts (Already applied).

**Retention Policy**: Feel free to delete if not needed for historical debugging.
